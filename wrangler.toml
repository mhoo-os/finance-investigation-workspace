name = "finance-investigation-workspace-synthetic"
main = "src/worker.js"
compatibility_date = "2026-09-03"

# Binding names only. This file intentionally has no account ID, resource ID, route,
# deployment configuration, secret, or production environment.
[[d1_databases]]
binding = "DB"
database_name = "finance-investigation-workspace-local"
database_id = "00000000-0000-0000-0000-000000000000"

[[r2_buckets]]
binding = "EVIDENCE"
bucket_name = "finance-investigation-workspace-synthetic-local"
